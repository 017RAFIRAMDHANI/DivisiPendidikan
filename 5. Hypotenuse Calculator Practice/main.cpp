#include <iostream>
#include <cmath>

using namespace std;
int main(){
	
    double x = 3.999;
    double y = 5;
    double z;
    
   // z = max(x,y);
   // z = min(x,y);
   // z = abs(-3);
   // z = sqrt(9);
   // z = round(x);
   // z = ceil(x);
    z = floor(x);
    
    cout << z;
	return 0;
}